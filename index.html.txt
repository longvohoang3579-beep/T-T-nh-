<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gửi Quỳnh Như 💕</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #ffe6f2; /* Nền hồng nhạt */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            overflow: hidden; /* Để hiệu ứng rơi không bị thanh cuộn */
            text-align: center;
        }

        #main-container {
            background: white;
            padding: 20px;
            border-radius: 20px;
            box-shadow: 0 10px 25px rgba(255, 105, 180, 0.3);
            width: 90%;
            max-width: 400px;
            z-index: 10;
            position: relative;
            border: 2px solid #ff69b4;
        }

        img.hello-kitty {
            width: 150px;
            margin-bottom: 10px;
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        h2, h3 { color: #ff1493; margin: 10px 0; font-size: 16px; }
        
        .question-box { margin-bottom: 15px; text-align: left; }
        
        label { display: block; margin: 5px 0; color: #555; cursor: pointer; }
        
        textarea {
            width: 100%;
            height: 80px;
            border: 1px solid #ffb6c1;
            border-radius: 10px;
            padding: 8px;
            box-sizing: border-box;
            font-family: inherit;
        }

        button {
            background-color: #ff69b4;
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 25px;
            font-size: 16px;
            cursor: pointer;
            transition: transform 0.2s;
            width: 100%;
            margin-top: 10px;
        }

        button:hover { transform: scale(1.05); background-color: #ff1493; }

        /* Hiệu ứng màn hình tối và chữ rơi */
        #animation-layer {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: black;
            display: none; /* Ẩn mặc định */
            z-index: 100;
            overflow: hidden;
        }

        .falling-text {
            position: absolute;
            color: #ff69b4; /* Chữ màu hồng */
            font-weight: bold;
            font-size: 18px;
            user-select: none;
            white-space: nowrap;
            animation: fall linear infinite;
        }

        .star {
            position: absolute;
            background: white;
            border-radius: 50%;
            animation: twinkle 2s infinite;
        }

        @keyframes fall {
            0% { transform: translateY(-100px); opacity: 0; }
            10% { opacity: 1; }
            90% { opacity: 1; }
            100% { transform: translateY(110vh); opacity: 0; }
        }

        @keyframes twinkle {
            0%, 100% { opacity: 0.2; }
            50% { opacity: 1; }
        }

        /* Ẩn input radio mặc định */
        input[type="radio"] { accent-color: #ff69b4; }

    </style>
</head>
<body>

    <div id="main-container">
        <img src="https://i.pinimg.com/originals/16/06/17/16061756535565578799477025555577.png" alt="Hello Kitty" class="hello-kitty">
        
        <form id="loveForm" action="https://formspree.io/f/YOUR_FORMSPREE_ID" method="POST">
            
            <div class="question-box">
                <h3>Mấy bữa nay Quỳnh Như cảm thấy tui như thế nào? 🤔</h3>
                <label><input type="radio" name="Cam_nghi" value="Tốt" required> A. Tốt</label>
                <label><input type="radio" name="Cam_nghi" value="Không Tốt"> B. Không Tốt</label>
                <label><input type="radio" name="Cam_nghi" value="Bình thường"> C. Bình thường</label>
            </div>

            <div class="question-box">
                <h3>Cần tui thay đổi gì để Giáng sinh này tui có ny hong? 🎄</h3>
                <textarea name="Loi_khuyen" placeholder="Viết ngắn gọn khoảng 200 chữ thui nha..." required></textarea>
            </div>

            <button type="submit" id="btnSubmit">Gửi cho tui đi 💕</button>
        </form>
    </div>

    <div id="animation-layer"></div>

    <script>
        const form = document.getElementById('loveForm');
        const mainContainer = document.getElementById('main-container');
        const animationLayer = document.getElementById('animation-layer');

        // Danh sách các câu sẽ rơi xuống
        const words = [
            "yeu qnhu💕", "mot cô gái dễ thương😘", "mãi yeu💞", 
            "chúc ngày 3/12 vui vẻ🥰", "sớm qlaii voi tui 😻", 
            "nhớ em nhiều 💖", "xinh đẹp tuyệt vời 🌹", "công chúa của tui 👑"
        ];

        form.addEventListener('submit', function(e) {
            e.preventDefault(); // Ngăn form chuyển trang ngay lập tức để chạy hiệu ứng

            // 1. Gửi dữ liệu (Mô phỏng hoặc gửi thật qua AJAX)
            const formData = new FormData(form);
            fetch(form.action, {
                method: form.method,
                body: formData,
                headers: {
                    'Accept': 'application/json'
                }
            }).then(response => {
                // Dù gửi thành công hay thất bại, vẫn chạy hiệu ứng
                startAnimation();
            }).catch(error => {
                startAnimation(); // Chạy hiệu ứng ngay cả khi lỗi (demo)
            });
        });

        function startAnimation() {
            // Ẩn form, hiện màn hình đen
            mainContainer.style.display = 'none';
            animationLayer.style.display = 'block';
            document.body.style.backgroundColor = 'black';

            // Tạo sao trắng
            for(let i=0; i<50; i++) {
                createStar();
            }

            // Tạo chữ rơi liên tục
            setInterval(createFallingText, 300); // 0.3s tạo một dòng chữ mới
        }

        function createStar() {
            const star = document.createElement('div');
            star.classList.add('star');
            const xy = Math.random() * 100;
            const size = Math.random() * 3;
            star.style.width = size + 'px';
            star.style.height = size + 'px';
            star.style.left = Math.random() * 100 + 'vw';
            star.style.top = Math.random() * 100 + 'vh';
            animationLayer.appendChild(star);
        }

        function createFallingText() {
            const text = document.createElement('div');
            text.classList.add('falling-text');
            text.innerText = words[Math.floor(Math.random() * words.length)];
            
            // Vị trí ngẫu nhiên theo chiều ngang
            text.style.left = Math.random() * 90 + 'vw'; 
            
            // Tốc độ rơi ngẫu nhiên (từ 3s đến 8s)
            text.style.animationDuration = (Math.random() * 5 + 3) + 's'; 
            
            // Kích thước chữ ngẫu nhiên một chút
            text.style.fontSize = (Math.random() * 10 + 16) + 'px';

            animationLayer.appendChild(text);

            // Xóa phần tử sau khi rơi xong để nhẹ máy
            setTimeout(() => {
                text.remove();
            }, 8000);
        }
    </script>
</body>
</html>